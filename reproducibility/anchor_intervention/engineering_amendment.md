# E3 恢复工程检查修订

649535两链原检查失败，失败保留；649564固定训练链诊断：恢复前参数与optimizer全等，
pair/坐标/loss全等，梯度relative6.35e-10，参数最大差3.73e-9。
这定位到后向微小浮点差，未定位具体kernel，不能声称逐位续接整条训练轨迹。
新工程检查保留精确状态恢复和前向，补共同原始梯度下clip+AdamW参数/状态精确相同。
实际独立反传的relative1e-6、参数maxabs2e-8为工程浮点容差，单独声明为修改的检查，
不是原逐位检查通过。它们只检查训练链，且没有读取E3评测得分。
正式run.py、loss、optimizer、学习率、数据和执行科学锁全部未改；无需重训练旧模型。
