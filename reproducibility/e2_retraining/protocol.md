# E2 九组完整重训练（2026-09-25）

用户已明确批准立即提交九组，不继续估算成本。本轮是原 E2 的独立执行重复，
不是新目标、新旋转、新种子或独立科学确认。原九组结果保留，不择优、不合并为六种子。

- HPC3 / acd_u / H100，9 个单卡任务；不重新运行 E3。
- 原 Train96、seed 20260923/24/25、I/r20270107/r20270104，9 个带 C 模型。
- 每组从原初始化重新执行 1536 步；同一 AdamW、损失、顺序、随机条件、4 轮现场 anchor。
- 直接使用已通过的原 E2 诊断修复版本，训练前后向及优化器源码不修改。
- 每组原 Confirm96-B 的 96 个完整预测，共 864；全矩阵结束后统一评分。
- 对照为原 E1 固定无 C 模型，条件于固定无 C 基线；完整无 C 重训练不在本轮。
- 工程 R2：首种子 × I/两 R × 有无 C，共 6 条件；连续 32 步，以及独立
  16 步前缀、退出进程、从该前缀原子断点续到 32 步，共 384 工程更新。
- 恢复必须逐项精确恢复参数、优化器、CPU/CUDA RNG、数据位置及学习率。
  恢复后首个 forward 与该断点保存前的同输入 forward 坐标相对误差 ≤1e-6，
  loss 绝对误差 ≤1e-6 + 1e-6×|loss|；该额外 forward 不执行 optimizer step，
  并还原 RNG。独立两条 32 步轨迹的参数/损失差异如实记录，
  不将 FP32 完整训练轨迹逐位一致作为门槛，也不宣称其等价。
- 工程所有 loss、参数、预期参与梯度必须有限且连接；允许个别零梯度，
  C 首步可为零；冻结主干哈希不变，实际四轮 forward + 末轮 backward replay。
- R2 六条件通过才释放正式九组；工程失败不放行，不按 Dev 分数改配方。
- 每组正式运行独立目录、原子断点；保留错误，不跳目标/更新、不改配方。
- 原源码、模型、缓存只读复用并核对历史哈希。这不是独立安装环境或重新提取 ESM
  特征的 clean-room 复现；计划中的 R1 公共 FASTA→新特征重放和 P192 未由本次九组
  重训练冒充完成。训练标签仍由原完整结构生成，query 特征按原代码重建。
- 新预测同时导出全原子 CIF，以高精度十进制保留 FP32 坐标；独立解析 CIF 后
  核对坐标/掩码并评分，重建原 B_R、T_C 和 Native change，原 20k target bootstrap。

原科学锁继续标识原配方，另用 repetition_lock.json 封存本次代码、输出根、
父运行、R2 工程门槛和新任务身份。任何本轮产物不能覆盖父运行。
