**已完成更新：720/720、0失败，2026-09-22 23:55:58 HKT完成预测及评分。见[最终结果复核](results_review.md)。下文保留启动时快照。**

# DiamondHill：完整推理方向来源 × 幅度来源

用户释放DiamondHill后开始。**正式队列已于2026-09-22 23:48:21 HKT放行。**
状态采样 2026-09-22T15:50:21.633291+00:00（UTC）：192/720正式预测完成，
0失败。尚未统一评分，不据部分结构得分调整队列。

## 已锁定安排

Mini / Train384 / Full /1536；3训练种子×3已有旋转，与同种子Native配对。
24个已观察Confirm96-B目标，沿用候选固定hash排序、4历史长度层各6条。
不新增训练、目标、旋转或优化器更新。全c4/s5/单样本/seed101；完整序列。

| 内容 | 数量 |
|---|---:|
| 实际残差预审计 | 24×12 =288 |
| 正式NN/RR同后端基线 | 24×(3+9)=288 |
| 正式NR/RN混合预测 | 24×3×3×2=432 |
| 正式总量 | **720** |

纯NN在同训练种子的三个旋转配对中共用一次输出。不是新增独立样本。
部署在 `/media/PM982/engramfold/runs/full_inference_cross_20260922`，
`/home/pc/anaconda3/envs/fold/bin/python`，既有AMD Torch2.12/HIP7.14；
最多8逻辑MI250并行。controller359854。HPC3 A/B队列不受影响。

## 工程及复核结果

- 3个单元测试通过：完整pair残差缩放、NN/RR原输出保持、零范数拒绝、四格恒等式。
- 全288残差均非零且有限；24目标的query baseline在12个模型间hash一致。
- 最短/最长固定目标、四格共8份当前工程预测通过：完整输入输出、4次pair-stack调用、
  实际torch随机张量hash和前后RNG配对、冻结主干及writer。
- 混合范数最大相对误差 1.4e-11，方向余弦约1；未使用GT可观测残基mask。
- 纯模型对旧输出回放最大坐标相对L2 2.4e-06，在预定1e-3内；并非逐位一致，
  也不当成全体结构分数误差界。**正式仍重算全部288纯基线**。
- 四格评分源码已用合成数据独立复算；分层bootstrap每次各层6条、总24目标，20000次。
  合成检查的720计数不是额外真实预测。
- 放行后复核全部source hash无变化。当前执行锁：
  `89e9b8dbdf7f3d9f9af3f4c4acc496b772e86d971e53dc3bd16b083b6b68eb9c`。

## 修复与失败记录保留

1. pytest第一次被旧仓库pythonpath指向旧模块；在独立root及-c /dev/null下重跑通过。
2. v1预检发现 `sequence_length` 字段遗漏；补成原序列真实长度并调用正式loader验证。
   未改变目标或序列。v1没有结构预测。
3. v2的共同decoder校验碰到原地to_device后的GPU张量与CPU donor张量比较；
   改为detach后的CPU副本精确比较。推理计算不变，重新封存并重跑全部前置。

旧锁、源码、日志及已完成工程产物分别留在 `attempts/preflight_v1` / `preflight_v2`。
当前formal由v3完整门槛释放，不将失败版本称为通过，也不把工程预测计入720。
HIP错误最多重试两次，每次120秒；其他错误停止或按预锁推理失败规则保留，
无无限重试或自动调幅。总派发上限6小时。

## 预锁科学解读

主读数Edir必须与D_mN、D_mR、Eamp、交互I以及四格绝对分数一起报告。
方向来源是独立训练模型的实际残差，包含内容差异；不是同一向量的纯正交旋转。
幅度来源不等于大/小幅度；Full残差先形成再缩放，绝不缩放因子增量。
本轮Train384/Full不是v4 Train24/Tangent同一套模型的机制续证。
标签只进最终评分，不进范数或选择。只有全部正式尝试完成后才统一三指标评分。

输入、执行锁、范数审计及工程报告见本目录 `panels/` 和 `remote_v3/`；
源代码部署草案见 `staging/`。未改论文正文或既有主结果。
