# 单目标 OpenFold 完整预测复现 — 2026-09-24

已完成：新建 Python 3.12 venv（不继承 system-site-packages）、离线安装固定依赖、从锁定源码重新编译 OpenFold CUDA 扩展、公开 FASTA 重算 ESM2 特征、加载发布的适配器、四轮完整预测、导出全原子 CIF、独立进程固定对应评分。最后在本机再次直接读取 CIF 评分，结果相同。

- 目标：3gxb_A，184残基；按已有工程清单第一条选择，不按质量挑选。
- 模型：OpenFold/AF2 model_3_ptm，Train96/Full/1536 native_s20260923；零新增训练。
- 设备：hpc3/acd_u，H100 80GB；测试环境Torch2.7.1+cu128、CUDA12.8。
- 主干权重和adapter均冻结；全长184个CA输出，4次trunk及4次选定OPM调用。
- 推理程序没有参考结构参数，只有独立score.py读取参考。

| 核验量 | 实测 | 预先设置的容差 |
|---|---:|---:|
| 特征相对L2 | 3.09776988e-06 | 0.0001 |
| 坐标相对L2 | 0.000137269372 | 0.001 |
| 坐标逐分量RMS | 0.000684853258 | 0.02 |
| pair-lDDT绝对差 | 5.37056928e-05 | 0.0001 |

历史pair-lDDT为 0.568259935553，重现为 0.568206229860。这不是逐位坐标复现，也不是对全矩阵的误差上界。

## 失败与修正记录

- 646482：新环境首次完整import缺modelcif，尚未生成预测；已把modelcif/ihm写入发布requirements。
- 646494：离线依赖库缺ihm的msgpack依赖，尚未生成预测；补齐固定msgpack版本，并在CPU完整导入及sequence_features检查后再提交。
- 646570：完整预测和评分成功；三次作业不算三次结构验证。
- reference最初通过rsync保留了历史符号链接。已改为具有相同SHA的普通文件，并在本机使用独立CIF评分器重算通过；当前发布包不存在此作者路径依赖。
- 初次源码核对把发布的live_opm.py与最初Train96训练快照做字节比较，发现后来新增的rotated-G+分支。当前Native Factor计算分支未变；五个运行文件与Fresh96的已核验推理快照逐字节一致，OpenFold的81个Python源码文件也与历史底座一致。原失败记录保留，不能声称所有初始训练源码均字节相同。

## 发布入口与范围

[可移植入口](../reproducibility/openfold_single/README.md)包含命令、明确的上游来源、输入FASTA、精简adapter权重、参考与映射、完整输出示例、环境freeze与验证。AF2与ESM2大权重由使用者按原许可取得并核验SHA，未重新分发。

此交付是“一个固定模型、一个目标、新建隔离Python环境下的完整预测及主指标评分”。系统Python、驱动、CUDA与编译器是明确的主机先决条件，不称全新操作系统或容器复现。它不提供全部模型重训、全量面板重生成或跨硬件逐位一致性保证。
