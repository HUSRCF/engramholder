# E2 第96步诊断分支修复与恢复

原数组648009的9个实例均失败于第96次optimizer更新之后、定期诊断写入和checkpoint96保存之前。日志已完成1–95步，最后原子断点均为0；不可声称从96步续训。

原因是诊断局部变量C被转为FP64，而torch.ones默认FP32，矩阵乘法拒绝混合dtype。两个torch.eye同时显式指定C.dtype以统一诊断精度。此修改只影响no_grad诊断记录，不改变实际训练中的C、参数、梯度、损失、优化器、学习率、clip或目标顺序。

之前两步smoke未覆盖每96步执行的诊断分支，这是工程覆盖遗漏。H100作业648047直接执行原/修复后的该AST分支：零通道参数和两个非零幅度共3例均复现原错误，修复后通过原正交/均值阈值1e-3，参数和RNG保持不变。9份原子断点及optimizer/RNG恢复字段已核对。

原封存源码与锁不改；隔离派生runner、修订SHA及原始日志另存。确认9任务已终止后，各自从正式checkpoint0按原种子/数据顺序重放，这是一次工程重试，不新增科学模型。原95步日志和断点均归档，不跳样本或优化步骤，不声明完整轨迹逐位复现。

旧评分依赖648010永远无法满足；新的评分作业依赖恢复后的整组完成，仍使用已经核验的CPU评分修订版。最终解释与选臂仍按原E2协议。
