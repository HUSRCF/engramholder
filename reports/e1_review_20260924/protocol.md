# E1 → E2 前瞻方向系列：执行协议 v1

依据写作仓库 `9d87049` 的 `prospective_orientation_series_20260924.md`，作者2026-09-24明确批准全部矩阵，要求先执行E1/E2。本执行范围为E1的24个768步writer探针、24个1536步结构训练，以及E2的9个1536步结构训练；不缩减旋转数。E3/E4已获作者预算授权但本次不提交，分别保留原科学门槛和后续独立锁。

既有矩阵统一评分→Dev8节点曲线→18组带符号置换以及三底座几何/传播先完成。新协议可立即CPU准备并进入持久等待队列；新GPU工作仅在E0收齐后放行，不取消/抢占原任务，也不读取E0分数来改变配置。

E1固定OpenFold model_3_ptm / Train96 / ESM2 layer12 / Full /1536，48 Evoformer、四轮现场anchor；历史AdamW θ参数lr1e-4、wd.01、betas(.9,.999)、eps1e-8，全局clip1，种子20260923/24/25，沿原schedule与每步seed+step。TF32关闭、H100、已验证Torch2.7.1+cu128。

旋转先封存：B为固定Householder均值正交补；T*=SO(127)种子20270100；Q_j种子20270101..108，NumPy Generator PCG64 CPU FP64 Gaussian QR，R对角号规范和负行列式末列翻转。R=uuᵀ+B Q T* Qᵀ Bᵀ。保存FP64及实际FP32数组、库版本和SHA，验证正交/行列式/均值/trace/距恒等/谱/不重复；不按诊断表现重新采样。

X：继承六组重建的实际算法和104份公共anchor，使用原Train96 Native3教师，24个探针。Train24，固定768步Adam(.001,wd0,clip1)，同一2048有放回全序列pair采样、四轮同因子增量、固定全pair教师能量和训练侧tau，0/256/768全部记录。唯一预测量为768步Holdout72目标/轮/教师等权相对平方误差。全零参照、范数、夹角、拟合与Dev误差保留；不选节点。不得使用重建学生初始化结构训练。

D：原Native3在Dev8的完整现场推理，8个R额外作用于已形成完整残差；不更新参数。192旋转+24原生基线预测统一Dev评分。D为固定pair-lDDT平均损害。除X、D不增加排序器。

预测锁在任何新R正式结构训练之前生成：X/D全部值与排序、测量分辨率、源hash、UTC；高低X各选一个R，并列按旋转ID。X精确常量、非有限、未完整覆盖则预测检验/E2不释放；跨度若≤1e-5（高于工程1e-6误差尺度）标记不可可靠分辨，不通过事后放宽。此工程可分辨门槛先锁定，非通过观察方向效果选择。

E1全部8R×3seed从历史encoder初始化/零输出层开始训练，每个24模型Confirm96-B96预测，共2304。预测过程中只检查完整输入输出、有限性与文件SHA，不读取评测标签或评分。全部正式尝试完成后CPU统一评分。Native3/Query既有384输出须逐个通过原来源/序列/坐标/hash审核并由工程身份路径回放；失败阻止释放，不自动更换模型或挑目标。若需重新预测，先单独记录工程修订。

主检验8旋转为单位，Spearman(X,Y)的40320个全排列单侧独立性检验；D为竞争探索项，不能替换X。所有96目标同步bootstrap20000次(seed20260924)，报告条件敏感性、三训练seed、留一教师排序、全部绝对值与相对Query；常量/NA/缺训练不做成功子集检验。结构推理失败保留目标、主分数计零，不删难例；工程失败日志与实际尝试单独保存。

E2独立锁后运行Native+C、X最低R+C、X最高R+C，各3seed共9组；864个Confirm96-B预测。按用户本轮批准，将其作为**已批准的独立增容干预**，即使E1相关性阴性也可在X有效时执行；不得把这称为“预测假说已通过”。高低R在E1预测锁已选定，不能按E1结构分数改变。

C=I+B(exp(A)-I127)Bᵀ，与uuᵀ+Bexp(A)Bᵀ代数相同，零A精确恒等；8001独立上三角参数，A反对称。完整残差后施加C，Native为CΔU，Rotated为CRΔU，baseline不变。A零初始化，独立AdamW组lr1e-3、wd0；θ沿原配方，统一clip1。C不从Rᵀ热启动，不扩大θ学习率/步数。梯度要求连接与有限，允许零输出起点A梯度为零并统一记录。记录C正交误差、均值、位移与实际A/θ梯度；没有严格FP32轨迹等价门槛。

工程固定Train96首链和最长链：全部八R零输出回放、实际结构反传，E2零C身份及非零teacher的逆旋转吸收/VJP/范数/均值、参数/optimizer恢复、冻结边界；历史Native新包装与原包装应精确回放。不用Dev结构质量调工程阈值。工程失败需留痕修复重锁，不改科学对象。

E2报告T_C（固定方向代价减learned-C方向代价）和B_R（旋转自身绝对改善），两R分别、Native自身变化同时保留；目标bootstrap20000 seed20260924。只有Native退化导致差距缩小不能称有效补偿。结果不证明X唯一因果因素或所有旋转可被有限训练消除。

资源：hpc3/acd_u，前序全部完成后最多8个并行H100任务；工程单卡30m×2，探针24×30m预约，Dev27×30m预约；正式单实例6h上限（含96预测），24+9个，保留实际训练/预测耗时。预约不是ETA。CPU封存/评分最多30m每阶段，低优先级；资源不足只排队，不换后端。每96步原子断点，0/384/768/1536另存；保存optimizer与随机状态，原per-step RNG/schedule不变。任务失败保留并停止自动科学放行；不换seed/旋转。恢复只允许同锁同任务，最多一次经明确终止状态确认的工程重试；不得因轮询超时重复提交。

统计、模型和调度各自hash封存；所有脚本及日志有独立根目录。当前任务跟踪与待汇报清单见`../execution_board_20260924.md`。论文正文与已封存结果不修改。

实现依据：[PyTorch matrix_exp](https://docs.pytorch.org/docs/stable/generated/torch.linalg.matrix_exp.html)与[正交参数化](https://docs.pytorch.org/docs/stable/generated/torch.nn.utils.parametrizations.orthogonal.html)。实际以本次固定Torch2.7.1运行及工程导数测试为准。
