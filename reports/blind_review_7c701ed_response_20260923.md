# 7c701ed盲审意见落实（2026-09-23）

最初依据用户贴出的意见修订；随后用户将完整盲审与训练预算报告复制到本仓库 `reports/`，现已阅读两份文件并核对遗漏。最初未找到文件的状态不再有效。本轮只写作与历史记录核验，不改实验队列。

## 已修改

- 数据规模段明确：OpenFold短链Factor方向差缩小、Protenix短链扩大、OpenFold长链未建立变化。不采用统一“小数据原生偏置有利”或“数据会补偿旋转”解释。数据覆盖、固定更新预算下的训练充分程度、训练集组成是尚未分离的候选因素；不靠结果正负切换解释。
- G+只证明自身函数类可吸收输出旋转，不证明Factor函数类包含于G+。正文明确其不是表达能力或性能上界；两头还改变逐残基/逐对组织、非线性与anchor入口。交互比较两个完整参数化，不能唯一归因固定decoder。
- 引言与方法分开两层控制：同一头共同状态的Native/Rotated具有局部谱性质；Factor/G+仅匹配初始函数、近似参数量与共同配方，不匹配Jacobian或有效学习尺度。
- 核心结果旁增加Confirm96-B的逐残基lDDT交互 +0.01737 [0.00571,0.02996] 和TM-score +0.01847 [0.00250,0.03528]，由既有锁定JSON生成6个新字段；说明这些是同批结构的指标稳健性，不是独立样本。
- 正文补Confirm96-A/B的实验结构来源、时间/质量条件、长度配额及嵌套/ID隔离；附录增加六列数据构成表，单列继承Train24/Dev8、Train96新增72条及Train384新增288条、确切哈希排序、序列筛查分母与首次评分身份。特别区分继承的Train24/Dev8与新候选截止条件。

## 数据核验范围

直接核对split96、split384、原cache、Train96/384清单、Confirm96-A原manifest与跨底座evaluation144。嵌套与Train384对Dev8/A/B/Length48的目标ID无交集均通过；A/B各4层24条、每PDB一链、存储质量字段通过。Train24/Dev8分别每层6/2条；Train96/384每层24/96条。实际Train384长度152–383，A为166–375，B为155–380。并核对A manifest与Train96/Dev8清单哈希及A/B协议哈希。

这不是新的序列搜索、家族隔离证明或历史时序认证。协议原文与audit脚本归档于 `reports/data_definition_sources/`；精简逐目标audit在 `evidence/data_composition_audit.json`。来源锁升级v5，仅新增该数据audit；v1–v4与全部旧输入哈希保留。

## 尚未解决

当前纳入稿件的中心交互仍缺新目标独立确认；本轮没有导入或评判原线程的新面板结果。更具体的条件依赖机制、训练充分程度与数据覆盖的分离、单目标端到端复现入口均未因修文而解决。不新增训练来回应表述问题。

## 验证

449字段生成通过，旧443字段及其来源逐项未变；数值键3测试、匿名包算子5检查通过。Tectonic编译成功，无未定义引用和overfull，主文实际止于9页。新增数据表已做渲染检查。完整文献、负结果和G+实用对照保留。匿名分析候选更新为v12，不冒充完整训练/预测复现。

## 后补训练预算报告

已阅读 [预算检查](openfold_training_budget_review_20260923.md)：报告核验30套历史schedule中Train96每链16次、Train384每链4次，并指出训练损失后段仍下降且没有正式逐checkpoint Dev结构曲线。本文只新增“1536步未建立收敛”这一限制，不将损失下降解释为更长训练必然改善结构或恢复交互。本写作线程未复查远端30套日志，不冒称独立重新执行了该检查，也不加入其训练loss表作为新主结果。
