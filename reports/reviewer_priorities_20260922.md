# 三项审阅意见：完成情况与下一项投入优先级

**最终更新：匹配Protenix G+已完成并核验。** 原先Train384/Length48缺格已补齐；两个面板Native−G+为+0.07630、+0.08138，区间均正。此前建议与启动记录作为历史保留。[完整结果](protenix_gplus384_results_20260922.md)。

依据9月22日刚完成的OpenFold旋转G+、AtlasFold正式矩阵和ESMFold2结果更新。这里只做证据与下一步判断，**Protenix匹配G+已于后续获准启动工程前缀检查；不改写论文正文**。

## 1. 完整的同输入比较：展示已补齐，Protenix实验缺项尚在

[主报告覆盖表](evidence_review.md)已经同时列出Query-only、Native、Rotated、G+及缺格。
Query-only不接入额外ESM2，其他三类有；同序列输入来源不等于完全相同的中间表征。
旧Generic没有显式query factors，不能充作G+。

- OpenFold Train96/384两面板四格已有；Train96新增旋转G+也已齐，直接交互为正。
- AtlasFold Train96两面板四格现已齐，未建立Native方向或适配收益。这一边界应与正结果同表展示。
- Protenix G+只有独立Train24/384选配方的Confirm96-B结果，不能拼入现有方向矩阵。
- **Protenix Train384/1536的G+，尤其Length48，仍是实质缺口。**报告明确缺项不是补齐实验。

若只允许新增一项训练研究，我优先考虑**匹配Protenix Train384/1536的G+三种子**：复用Native/Rotated/Query，三个G+使用相同训练集合、种子、任务顺序、输入特征、损失与预算；公共学习率按既有该配方锁定，不再按新面板调参。最终checkpoint1536，两面板合计3×144=432次新预测，训练3×1536=4608更新。

这是一个训练配方下的受控方法比较，不宣称G+已充分调优。旧Train24 G+的独立选LR结果仍保留为不同研究。新增结果为负也有价值：它回答原生约束的竞争边界。**不要求Native赢G+，也不自动加入旋转G+、Train96额外矩阵或新面板。**

2026-09-22用户随后认可该优先级并明确要求继续。已封存协议、启动三个种子的正式两步前缀工程检查；均通过历史配对与梯度核验，完整推理工程检查亦已通过，11:26 HKT释放三组正式续训。结果格仍标记“待完成”，不能以启动代替结果。见 [执行状态](protenix_gplus384_status_20260922.md)。

## 2. 可审查的数学对象：已有资料已补齐，优先使用而非新增实验

[数学契约](math_contract.md)已给出query/ESM路径、固定原生算子、残差注入、冻结边界、旋转位置、末层零初始化，以及Full中的二阶项与Tangent的区别。

D=508.5直接由原Train24的24个teacher_depth复算，中间508、509；这是训练侧历史MSA元数据固定常数，不是测试信息，也不是推理输入行数。原始depth=1 query baseline独立保留。OpenFold/Atlas不复用该数值。

扩样交互必须使用同为Full/1536的Train96与Train384。两个点和逐目标直接交互都已列出，绝不使用Train24 tangent/384来替代Train96。

这部分最可能减少技术可信度上的疑问，目前无需再开训练。用户认可报告后再转入正文；现在不提前写论文。

## 3. 聚合稳健性：已有直接核验，也必须保留无法证明的部分

[稳健性审计](robustness_audit.md)已复算12个旧比较的目标数组、种子/旋转边际、9单元、分位数和正向比例，误差<1e-12；主锁定与后续观察身份、ID隔离、累计暴露和CATH未审计范围明确。

最新结果继续采用同样透明度：

- OpenFold G+交互：主指标两面板三种子/三旋转边际均正，交叉单元8/9和7/9；不是每个实例都正。
- AtlasFold Native−Rotated：两面板交叉单元3/9和1/9为正；没有稳定原生优势，不能将它写成跨底座成功。
- 目标bootstrap条件于固定拟合模型；三训练种子不能充分量化全部重新训练不确定性。
- 目标ID不重合、序列筛查和严格家族隔离不是同一保证。632条已列query记录也不是全部历史teacher/调参暴露认证。

这些分析能检验某些“只靠个别实例/目标”的解释，**不能证明结果与所有汇总方式无关，也不能消除后续研究选择偏差**。不再用新的子集、分组或删离群值来追阳性。

## 判断

最有价值的新增实验缺口是匹配的Protenix G+；最有价值的报告改进是把已完成的数学与完整正负结果真正放在同一视野。OpenFold正交互提高了对照的辨别力，AtlasFold边界提高了主张的准确性；二者都不支持“所有模型必然有效”或“Factor普遍最佳”。这些可以提高证据的可信度，但无法保证评审分数或接收。
