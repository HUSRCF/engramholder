\section{Results}
\label{sec:results}
\subsection{Rotation sensitivity interacts with adapter parameterization}
\label{sec:interaction_result}
Under the matched OpenFold Train96/ESM2-35M recipe, output rotation affects the factor
head more than G+ (Figure~\ref{fig:interaction}). On Confirm96-B, the factor
contrast is \result{inter_c96_factor_rotation}, while G+'s contrast is
\result{inter_c96_gplus_rotation}. The directly estimated interaction is
\result{inter_c96_interaction}
[\result{inter_c96_interactionLo}, \result{inter_c96_interactionHi}].
The Length48 secondary comparison is also positive:
\result{inter_l48_interaction}
[\result{inter_l48_interactionLo}, \result{inter_l48_interactionHi}].
These are differences-in-differences on a 0--1 score scale, not relative
percentage improvements. The completed Train384 four-cell controls do not
establish the interaction with either ESM2 or ESMC
(Table~\ref{tab:interaction_scope}); the positive finding is recipe-specific.

The main-metric interaction is positive in all three seed marginals and all
three rotation marginals on both panels, but in 8/9 and 7/9 individual
seed-by-rotation cells, respectively. Target-level interactions are positive
for 61/96 and 34/48 proteins. On Confirm96-B, the residue-average lDDT interaction is
\result{inter_c96_interaction_residue}
[\result{inter_c96_interaction_residueLo}, \result{inter_c96_interaction_residueHi}],
and the TM-score interaction is \result{inter_c96_interaction_tm}
[\result{inter_c96_interaction_tmLo}, \result{inter_c96_interaction_tmHi}].
These supplementary metrics score the same structures, not independent samples.
The Length48 supplementary intervals are positive, although one TM-score seed
marginal is negative. The effect is therefore not confined to a single marginal instance,
and it is not positive for every fitted combination.

G+'s direction intervals include zero on both panels; its Length48 rotated mean
is slightly higher. This does not establish equivalence of trained G+ systems.
Instead, the direct interaction shows different rotation sensitivity under the
tested parameterizations and optimizer. The function-class control rules out a
loss of representable functions as a necessary effect of rotating G+, while
leaving optimization dynamics as part of the empirical comparison.

\begin{figure}[t]
\centering\includegraphics[width=\linewidth]{figures/interaction.pdf}
\caption{OpenFold Train96/ESM2-35M four-cell experiment. Left: absolute mean scores
(F: factor, RF: rotated factor, RG+: rotated G+). Right: paired factor and G+
direction effects and their direct interaction $\Psi$, with 95\% target-bootstrap
intervals conditional on fitted models. Both panels were previously observed;
Confirm96-B is the primary endpoint of this follow-up. Completed Train384
interactions for both PLMs are reported in Table~\ref{tab:interaction_scope}.}
\label{fig:interaction}
\end{figure}

\subsection{Native orientation benefits factor adaptation in tested Protenix settings}
\label{sec:protenix}
The original Protenix-Mini tangent comparison on Confirm96-B gives a native
advantage of \result{pt_tangent} [\result{pt_tangentLo}, \result{pt_tangentHi}],
with 78/96 positive targets. Full gives \result{pt_full24}
[\result{pt_full24Lo}, \result{pt_full24Hi}]. The tangent construction serves to
isolate first-order updates; it is not a uniformly better adapter than Full.
Using Tiny's own factors and decoder retains an advantage of
\result{pt_tiny} [\result{pt_tinyLo}, \result{pt_tinyHi}], extending the evidence
to a second checkpoint within the same family.

A mean-preserving orthogonal control, $Ru=u$ for the all-ones unit vector $u$,
also underperforms native updates: \result{pt_mean}
[\result{pt_meanLo}, \result{pt_meanHi}]. Preserving this special channel
direction therefore does not remove the effect in the tested setting. It does
not isolate all downstream normalization behavior. These follow-ups, together
with the Full training-scale comparisons, are shown in
Figure~\ref{fig:scope}. They establish a scope for the factor direction effect;
they do not constitute additional factor-versus-G+ rotation interactions.

\subsection{Fixed adapters transfer beyond their adaptation-training lengths}
\label{sec:length}
Without further training, the fixed Train384 Full adapters retain an advantage
on Length48 of \result{pt_length} [\result{pt_lengthLo}, \result{pt_lengthHi}],
with 41/48 positive targets. Native and rotated means are
\result{pt384_l48_native} and \result{pt384_l48_rotated}, both above the
unadapted mean of \result{pt384_l48_query} (Table~\ref{tab:coverage}).
Thus the contrast is accompanied by useful adaptation in mean score, rather
than only less damage from one update family. Baseline comparisons here remain
descriptive secondary results. Supplementary residue-average lDDT and TM-score
support the same direction.

This study tests complete sequences beyond the adapters' training-length
range, not arbitrary-length prediction or family novelty. The pretrained
folding model and PLM may already have encountered long or related proteins.
Official Mini-ESM scores \result{pt384_l48_official} on the same panel, leaving
a substantial quality gap. Length transfer strengthens the tested applicability
of the adapter but does not establish a competitive replacement for the full
native system.

\begin{figure}[t]
\centering\includegraphics[width=\linewidth]{figures/scope.pdf}
\caption{Scope of native-minus-rotation effects. P: primary comparison on a newly
locked panel at the time of that study; S: prespecified secondary comparison in
the original Protenix direction study; F: subsequent observed-panel study or
extension. The mean-preserving row uses Train24/tangent/384. All intervals are
conditional target-bootstrap intervals; rows share targets and are not
independent replications. Null and negative conditions are retained. OpenFold labels distinguish ESM2
from ESMC features; this figure shows $\Delta_F$, not $\Psi$.}
\label{fig:scope}
\end{figure}

\subsection{Feature substitution improves scores without establishing an interaction}
\label{sec:esmc}
The OpenFold ESMC feature-substitution study (Stage A) completes the Train384
four-cell comparison for ESM2-35M
and repeats it with frozen ESMC-600M final-layer features. It adds 33 fits and
4,752 predictions, all successful, while retaining the final 1536-update rule
and common AdamW recipe. Both panels are observed follow-ups.
ESMC raises Native's mean score by \result{a_c96_native_gain} on Confirm96-B
and \result{a_l48_native_gain} on Length48; G+ improves by
\result{a_c96_gplus_gain} and \result{a_l48_gplus_gain}, respectively.
Table~\ref{tab:esmc} reports the complete four cells and the unchanged baseline.

\begin{table}[t]
\caption{OpenFold Train384/1536 feature-substitution study: absolute pair-lDDT means under each
adapter feature source. F/RF are Native/Rotated Factor; G+/RG+ are G+/Rotated G+.
The query-only baseline has no added adapter PLM. Historical ESM2 F/RF/G+ and
query predictions are reused; the other cells are newly trained and evaluated.}
\label{tab:esmc}\centering\footnotesize
\begin{tabular}{llrrrrr}\toprule
Panel & Features & Query & F & RF & G+ & RG+ \\\midrule
\input{generated/esmc_A_means}
\end{tabular}
\end{table}

Higher quality does not establish stronger direction dependence. The ESMC
Native--Rotated primary contrast is \result{a_c96_c_ca_lddt_factor_rotation}
[\result{a_c96_c_ca_lddt_factor_rotationLo}, \result{a_c96_c_ca_lddt_factor_rotationHi}]
on Confirm96-B, and the secondary Length48 contrast is
\result{a_l48_c_ca_lddt_factor_rotation}
[\result{a_l48_c_ca_lddt_factor_rotationLo}, \result{a_l48_c_ca_lddt_factor_rotationHi}].
Both intervals include zero, as do the ESMC head-by-rotation interactions
and the direct changes in direction effect and interaction relative to ESM2
(Appendix~\ref{app:esmc}). G+ retains the higher mean on both panels.
Thus the stronger feature source improves this adaptation recipe without
establishing preservation or removal of the rotation effect. This substitution
changes PLM family, feature width, cache precision and trainable input-projection
size together; it neither isolates a
model-size effect nor tests intermediate-layer or multilayer features.

The completed ESM2 Train384 control also does not establish an interaction on
either panel (Table~\ref{tab:interaction_scope}). The earlier positive result
concerns Train96; neither this comparison nor the direct PLM interaction
change identifies ESMC as the reason for the lack of an established interaction.
The training-scale contrast reported below concerns $\Delta_F$, not $\Psi$;
it cannot establish a change in the full head-by-rotation interaction.

\begin{table}[t]
\caption{Completed OpenFold head-by-rotation interactions $\Psi$, with 95\%
target-bootstrap intervals conditional on fitted models. All rows use 1536
updates on observed panels. Train96 retains its original intervals; Train384
uses the feature-substitution study's bootstrap stream. These are not direct
tests of differences between rows.}
\label{tab:interaction_scope}\centering\footnotesize
\begin{tabular}{lll}\toprule
Training set / features & Confirm96-B & Length48 \\\midrule
\input{generated/interaction_scope_rows}
\end{tabular}
\end{table}

\subsection{The learned-source gap persists at matched global residual norms}
\label{sec:norm_cross}
A full-inference robustness analysis crosses learned residual source and global
Frobenius norm on 24 observed Confirm96-B targets, using fixed Protenix
Train384/Full/1536 models. All 720 pure and mixed predictions are rerun on the
same backend. Native-source advantages are \result{cross_ca_D_mN} at the
native norm and \result{cross_ca_D_mR} at the rotated norm; the symmetric
source effect is \result{cross_ca_Edir}
[\result{cross_ca_EdirLo}, \result{cross_ca_EdirHi}]. Thus global norm alone is
insufficient to explain this gap. The normalized residuals still differ in
learned content, local magnitudes and channel organization: this is not a
second pure channel-rotation experiment. The complete four cells, conditional
effects and scope are retained in Appendix~\ref{app:norm_cross}.

\subsection{Competitiveness and limits across predictors}
\label{sec:limits_results}
Table~\ref{tab:coverage} places direction effects next to absolute performance
and the generic comparator. In the matched Protenix Train384 follow-up, Native
exceeds G+ by \result{pt384_c96_gdiff}
[\result{pt384_c96_gdiffLo}, \result{pt384_c96_gdiffHi}] on Confirm96-B and
\result{pt384_l48_gdiff}
[\result{pt384_l48_gdiffLo}, \result{pt384_l48_gdiffHi}] on Length48.
The differences are positive for 78/96 and 43/48 targets and all three paired
seed means. G+ itself improves over query-only by \result{pt384_c96_gquery}
and \result{pt384_l48_gquery} in mean score. Thus Native's advantage holds
against a functioning explicit-factor generic comparator under this common
learning rate and budget, not against a separately optimized G+ upper bound.
The long-chain comparison combines historical H100 predictions with new MI250
G+ predictions; replay checks are limited engineering evidence, not a
panel-wide numerical error bound (Appendix~\ref{app:engineering}).

OpenFold Train384 gives
\result{of384_c96_direction}
[\result{of384_c96_directionLo}, \result{of384_c96_directionHi}] on Confirm96-B,
without an established advantage. On Length48 it gives
\result{of384_l48_direction}
[\result{of384_l48_directionLo}, \result{of384_l48_directionHi}].
G+ exceeds the native factor head on the two OpenFold long-chain comparisons;
Appendix Table~\ref{tab:gplus} gives all paired competitiveness estimates. A direction benefit within
one constrained family can coexist with a better generic adapter.

\begin{table}[t]
\caption{Absolute C$\alpha$ pair-lDDT means. All rows use 1536 task updates;
Protenix/OpenFold use Full. Native and G+ average three seeds; Rotated averages
three seeds and three rotations. C96-B: Confirm96-B; L48: Length48.
Unadapted is the query-only baseline for Protenix/OpenFold and retains native
AtlasLM for AtlasFold; it does not receive the added adapter ESM2 features.
--- indicates no matched experiment.}
\label{tab:coverage}\centering\footnotesize
\begin{tabular}{llrrrr}\toprule
Backbone / recipe & Panel & Unadapted & Native & Rotated & G+ \\\midrule
\input{generated/main_table_rows}
\end{tabular}
\end{table}

AtlasFold's unadapted means are \result{atlas_c96_query_native_plm} and
\result{atlas_l48_query_native_plm}. Its tested adapters yield little
native--rotation separation: \result{atlas_c96_direction}
[\result{atlas_c96_directionLo}, \result{atlas_c96_directionHi}] and
\result{atlas_l48_direction}
[\result{atlas_l48_directionLo}, \result{atlas_l48_directionHi}], respectively.
The configurations establish neither a native-direction benefit nor improved
adaptation over that baseline. This is a boundary of the tested interface and
recipe, not evidence that only weak systems can benefit from adaptation.



The fixed-budget data interaction also differs by predictor. Using Full and
1536 updates on both sides, $(N-R)_{384}-(N-R)_{96}$ is
\result{pt_scale} [\result{pt_scaleLo}, \result{pt_scaleHi}] for Protenix on
Confirm96-B, versus \result{of_scale_c96}
[\result{of_scale_c96Lo}, \result{of_scale_c96Hi}] for OpenFold.
The corresponding OpenFold Length48 estimate is
\result{of_scale_l48} [\result{of_scale_l48Lo}, \result{of_scale_l48Hi}].
These direct interactions concern changes in training-set composition and
repetition at fixed update count (16 versus 4 average passes). The short-chain factor gap shrinks in OpenFold but grows in Protenix;
OpenFold Length48 establishes no change. A general rule that more data
compensates for rotation is unsupported. Broader data coverage, the degree
of optimization at fixed updates, and training-set composition remain
unseparated candidate explanations, not interchangeable explanations of either sign.
