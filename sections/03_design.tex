\section{Experimental design}
\label{sec:design}
\paragraph{Predictors and information.}
We freeze Protenix-Mini/Tiny default checkpoints, official AF2
\texttt{model\_3\_ptm} weights through OpenFold, and AtlasFold with native
AtlasLM-3B retained. Folding inputs exclude evaluation homologs, templates and
target structures. Native/Rotated/G+ share frozen ESM2-35M features
\citep{lin2023evolutionary} (480 channels, layer 12), or frozen ESMC-600M final-layer features
\citep{candido2026language} (1152 channels; Section~\ref{sec:esmc}).
Unadapted baselines lack these added features: gains over baseline combine
feature access and adaptation; Native--Rotated isolates direction more closely.
Table~\ref{tab:recipes} summarizes the paths.

\begin{table}[t]
\caption{Core configurations. We transfer a residual construction, not a universal
normalization or loss. All folding and PLM weights are frozen.}
\label{tab:recipes}
\centering\footnotesize\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{.16\linewidth}p{.25\linewidth}p{.29\linewidth}p{.19\linewidth}}
\toprule
Predictor & Interface / anchor & Task supervision & Structure inference \\
\midrule
Protenix & Single MSA-block OPM; cached anchor/update & $4$ MSE + $4$ bond + $4$ smooth-lDDT + $0.03$ distogram & 4 trunk passes; 5 diffusion steps \\
OpenFold & First of 48 Evoformer OPMs; recycled-state anchor & FAPE + $0.3$ distogram + supervised-chi & 4 trunk passes; structure module \\
AtlasFold & First LM difference/product; MLM anchor, no adapted history & $0.4$ distogram + $2$ (weighted MSE + smooth-lDDT) & 5 trunk passes; 20/30 diffusion steps$^*$ \\
\bottomrule
\end{tabular}
\par\smallskip\footnotesize $^*$20 steps for $L\le512$ and 30 for $513\le L\le1024$,
fixed before evaluation. Protenix's protein-only bond mask is empty in this
setting. Confidence losses are excluded. Full recipes are in Appendix~\ref{app:recipes}.
\end{table}

\paragraph{Training and pairing.}
The Protenix direction study uses Train24/384 updates, with tangent as the
locked main comparison and Full as a secondary comparison. Full Train96/1536
and Train384/1536 test larger sets at a fixed update budget. OpenFold uses Full
Train96 and Train384, each for 1536 updates; AtlasFold uses Train96/1536.
Within each recipe, heads share data order, three training seeds, random-condition
rules and optimizer settings. Each of three fixed rotations uses all three seeds;
G+ also has three native and nine rotated fits in each completed four-cell setting.
We evaluate the fixed final checkpoint and one inference sample, without
target-label selection or cropping. Native task losses backpropagate through
the final recycle.

\paragraph{Panels and study identity.}
\label{sec:panels}
Confirm96-A supports the historical Factor--Generic comparison; Confirm96-B
supports the original Protenix direction comparison. Both contain 96 short-chain
targets selected from the existing experimental-structure catalog: single-model
X-ray entries released by 2021-09-30, resolution at most 2.5\,\AA, standard
amino acids and at least 90\% mapped C$\alpha$ coverage. Four length strata
(128--191, 192--255, 256--319, 320--384) contribute 24 targets each.
Table~\ref{tab:data_composition} distinguishes their construction and first use. Length48 comprises 16 targets in each prespecified range 385--512,
513--640 and 641--768; actual lengths are 392--750, beyond the Train384 adapter
training range of 152--383. The original direction and length studies locked new targets before scoring;
later uses of those panels are observed follow-ups. Table~\ref{tab:study_roles}
separates panel novelty from each four-cell study's endpoint role. The channel-compensation
study predeclares mean benefit and gap reduction over two selected rotations;
its observed-panel intervention, the eight-rotation prediction test and Dev8
checkpoint diagnostics retain distinct roles (Appendices~\ref{app:prediction_e1}--\ref{app:e2_compensation}). Earlier
matched-G+ and norm-exchange endpoints remain in their respective appendices.
Train24 $\subset$ Train96 $\subset$ Train384; Train384 has no target-ID
overlap with Dev8 or either confirmation panel. The inherited Train24/Dev8
are not restricted by the new-candidate release cutoff. Sequence screening and
study-specific exclusions are recorded in Appendix~\ref{app:chronology}; they do not establish family or
foundation-model pretraining isolation.

\paragraph{Scores and uncertainty.}
Our primary score is C$\alpha$ pair-lDDT: for all fixed reference pairs within
15\,\AA, average distance-preservation credit at thresholds 0.5, 1, 2 and
4\,\AA, then give each target equal weight. Missing prediction pairs receive
zero credit without changing the reference denominator. This is a C$\alpha$
pair-averaged variant of lDDT \citep{mariani2013lddt}. Residue-average C$\alpha$
lDDT and fixed-correspondence TM-score \citep{zhang2004tm} are supplementary.
For target $i$ define
\begin{equation}
 d_{H,i}=\frac13\sum_s\left(S_{H,i,s}-\frac13\sum_rS_{H,R_r,i,s}\right),
 \quad \Psi_i=d_{F,i}-d_{G+,i},\quad
 \Psi=\frac1N\sum_i\Psi_i.\label{eq:interaction}
\end{equation}
We resample whole target records for 20,000 paired bootstrap draws, preserving
within-target seed/rotation pairing. Intervals condition on the fitted models;
seeds, rotations and supplementary metrics are not additional independent
proteins. Secondary and follow-up intervals are unadjusted; the OpenFold feature-substitution
study reports its own predeclared three-test Holm correction (Appendix~\ref{app:esmc}). Protocol-specific
bootstrap seeds and endpoint roles are preserved in the accompanying records.

\begin{table}[t]\centering\footnotesize
\caption{Identity of the complete four-cell studies. A study-specific primary
endpoint on observed targets is not new-target confirmation. C96-B denotes
Confirm96-B. All intervals condition on fitted models.}
\label{tab:study_roles}
\begin{tabular}{p{.22\linewidth}p{.13\linewidth}p{.25\linewidth}p{.27\linewidth}}\toprule
Study & Panel status & Declared primary & Role of $\Psi$ \\\midrule
OpenFold Train96 & Observed & C96-B $\Psi$ & Primary; L48 secondary \\
OpenFold signed permutations & Observed & C96-B $\Psi$ & Primary; L48 secondary \\
Protenix Train96 & Observed & C96-B $\Psi$ & Primary; no L48 study \\
OpenFold Fresh96 & New targets & Fresh96 $\Psi$ & Sole primary; fixed models \\
OpenFold features & Observed & C96-B ESMC $\Delta_F$ & C96-B ESMC $\Psi$: secondary (Holm); other $\Psi$ unadjusted \\
Protenix/Atlas features & Observed & C96-B ESMC $\Delta_F$, per backbone & Follow-up; no joint correction \\\bottomrule
\end{tabular}\end{table}
