\section{Related work}
\label{sec:related}
\placeholder{Add verified primary references in three short connected paragraphs:
(1) frozen representation adaptation, low-rank and orthogonal parameterizations;
(2) protein folding with MSA or native PLM representations and the implementations
actually tested; (3) training geometry and gradient/representation interventions.
Explain the distinction of a native internal operator plus paired trained output
controls. Do not claim the rotation reparameterization proof, gradient inner
product, or activation-space adaptation itself is new. Check citations before
adding bibliography entries; the template's example references are not evidence.}
