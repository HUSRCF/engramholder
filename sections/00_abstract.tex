\begin{abstract}
After task training, does the output orientation of structured updates still
affect adaptation in a frozen predictor, and can shared output freedom
mitigate the cost? We compose adapter residuals through protein predictors'
native pair-generating operators and independently train native and rotated
heads from the same zero-residual function. Orthogonal controls preserve
local writer spectra; a generic head with explicit factor access and a free
output layer provides a function class closed under output rotation.
Under fixed 1536-update recipes, matched four-cell studies on observed
panels find greater rotation sensitivity for Factor than G+ in both
OpenFold and Protenix under Train96/ESM2, with C$\alpha$ pair-lDDT
interactions of \result{inter_c96_interaction} and
\result{p96_pair_interaction}. On the observed panel, jointly learning a
shared orthogonal compensator reduces mean direction cost by
\result{e2_pair_T_C}
[\result{e2_pair_T_CLo}, \result{e2_pair_T_CHi}] across two preselected
OpenFold rotations. On 96 prospectively locked targets, however, fixed models
from the OpenFold four-cell study yield an interaction of
\result{fresh_ca_lddt_interaction}
[\result{fresh_ca_lddt_interactionLo}, \result{fresh_ca_lddt_interactionHi}];
the primary interaction is not established. A reconstruction diagnostic also
fails to predict retraining costs.
Across tested predictors and training settings, adaptation benefit, rotation
sensitivity, and head competitiveness remain distinct. These controls
identify a conditional design effect: structured interface evaluation must
consider output freedom and task training alongside local spectral
properties, without treating direction sensitivity as method superiority.
\end{abstract}
